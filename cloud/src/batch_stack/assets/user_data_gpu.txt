#!/bin/bash -ex

echo "AWS Batch for ML Training : GPU AMI installs the public epel repo by default. Need to remove it for private subnets "

yum-config-manager --disable epel

echo "AWS Batch for ML Training : GPU AMI - END"
